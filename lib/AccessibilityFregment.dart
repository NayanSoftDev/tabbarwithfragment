import 'package:flutter/cupertino.dart';

class AccessibilityFregment extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("AccessibilityFregment"),
      ),
    );
  }
}