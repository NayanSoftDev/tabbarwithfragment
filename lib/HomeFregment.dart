import 'package:flutter/cupertino.dart';

class HomeFregment extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("HomeFregment"),
      ),
    );
  }
}